<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Android 12 em diante desenha a abertura pela API do sistema e ignora o
             android:background abaixo, que só vale nas versões anteriores. Sem estes
             dois itens o resultado era o padrão do sistema: fundo claro e o ícone
             genérico do Capacitor, nada a ver com o app. -->
        <item name="windowSplashScreenBackground">@color/alicerce_navy</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_marca</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>